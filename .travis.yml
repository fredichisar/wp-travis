# .travis.yml

language: php

sudo: false

php:
  - 7.0
  - 7.1



deploy:
  skip_cleanup: true
  file_glob: true
  file:
    - travis_release/oyst.tar.gz
    - travis_release/oyst.zip


script:
  # Run your tests here.
  - echo 'Tested'